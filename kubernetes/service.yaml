apiVersion: v1
kind: Service
metadata:
  name: nginx-service
  namespace: devops-lab
spec:
  type: ClusterIP
  selector:
    app: nginx-demo
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
